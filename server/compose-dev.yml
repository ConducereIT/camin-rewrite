services:
  db:
    image: postgres
    restart: always
    shm_size: 128mb
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: example
      POSTGRES_DB: rezervariCamin
    volumes:
    - ../volumes/db:/var/lib/postgresql
  adminer:
    image: adminer
    restart: always
    ports:
    - 8030:8080
  # backend:
  #   image: blitzcloud/camin:0.0.2
  #   restart: always
  #   depends_on:
  #     - db
  #     - adminer
  #   ports:
  #     - 8881:8881
  #   env_file:
  #     - ./.env
